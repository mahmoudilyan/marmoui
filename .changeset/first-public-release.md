---
---

First public release. Publishes the versions already set in each package —
`@marmoui/ui@1.0.0` (React component library, MIT) and `marmoui@0.1.0`
(`npx marmoui init` agent setup CLI) — with no additional bump.
