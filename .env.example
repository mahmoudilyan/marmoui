# Copy to `.env.local` and fill in real values. NEVER commit `.env.local`.

# Figma REST API — personal access token from figma.com → Settings → Security
# Used by `pnpm figma:node` to fetch design specs without MCP rate limits.
FIGMA_TOKEN=
