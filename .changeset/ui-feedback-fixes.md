---
"@marmoui/ui": patch
---

Tooltip border uses the border token instead of currentColor (was rendering black); sidebar collapse toggle uses the Phosphor List icon instead of BookOpen; PageSection sticky header border softened to border-muted.
