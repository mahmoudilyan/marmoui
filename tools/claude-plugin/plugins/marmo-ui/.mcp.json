{
  "mcpServers": {
    "marmo-ui": {
      "type": "http",
      "url": "${user_config.mcp_url}",
      "headers": {
        "Authorization": "Bearer ${user_config.marmo_mcp_token}"
      }
    }
  }
}
